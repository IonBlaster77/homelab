apiVersion: v1
kind: Namespace
metadata:
  name: networking
  labels:
    # Pod security — nothing privileged runs here (just CRD-level Gateway objects)
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/enforce-version: latest
