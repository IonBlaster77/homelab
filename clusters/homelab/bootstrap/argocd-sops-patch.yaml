# DEPRECATED — superseded by argocd-values.yaml (repoServer.initContainers,
# repoServer.extraContainers, repoServer.volumes).
#
# The SOPS age key mount and sops binary installation are now declared in
# argocd-values.yaml so they are applied via `helm upgrade` rather than
# a manual kubectl patch. This file is kept for reference only and should
# not be applied to the cluster.
#
# Original content below — DO NOT APPLY:
#
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: argocd-repo-server
#   namespace: argocd
# spec:
#   template:
#     spec:
#       volumes:
#         - name: sops-age
#           secret:
#             secretName: sops-age
#         - name: custom-tools
#           emptyDir: {}
#       initContainers:
#         - name: install-sops
#           image: alpine:3.19
#           command: [sh, -c, "wget -qO /custom-tools/sops ... && chmod +x /custom-tools/sops"]
#           volumeMounts:
#             - name: custom-tools
#               mountPath: /custom-tools
#       containers:
#         - name: repo-server
#           env:
#             - name: SOPS_AGE_KEY_FILE
#               value: /home/argocd/.config/sops/age/keys.txt
#             - name: PATH
#               value: /custom-tools:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
#           volumeMounts:
#             - name: sops-age
#               mountPath: /home/argocd/.config/sops/age
#               readOnly: true
#             - name: custom-tools
#               mountPath: /custom-tools
