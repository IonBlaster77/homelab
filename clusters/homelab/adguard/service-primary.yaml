apiVersion: v1
kind: Service
metadata:
  name: adguard-primary
  namespace: adguard
spec:
  type: ClusterIP
  selector:
    app: adguard-primary
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
